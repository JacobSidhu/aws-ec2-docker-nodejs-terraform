# aws-ec2-docker-nodejs-terraform
Provision an AWS EC2 instance using Terraform, install Docker automatically, deploy a simple Node.js/Express app inside a Docker container, and document the deployment.
